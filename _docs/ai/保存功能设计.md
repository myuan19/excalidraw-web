希望实现统一机制，解决切后台保存与五秒定时保存可能存在的冲突问题。我担心两者同时触发会引发异常，因此建议将保存逻辑统一为“生产-消费”模式：触发后生成事件并放入队列，由统一的保存模块进行消费。若两个触发条件间隔极近或几乎同时发生，则合并执行一次保存操作。具体而言，将所有需要保存的事件统一投递至该队列中。这是我目前的思路，若您有更优方案也可另行设计。通过这种复用机制，未来新增任何保存触发条件时，只需让其产生对应的保存事件即可，后续的同步等流程均可自动化处理。

---

