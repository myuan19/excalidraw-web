# References

Non-mainline code kept for reference. Not part of the active build (`yarn start` / `yarn build`).

| Path | Contents |
|------|----------|
| `old2-drawing-space-shell/` | v0.3 React shell (Zustand + `src/features/*`) |
| `experiments/` | Multi-format PoC validation scripts (P0–P2) |
| `upstream/` | Upstream Excalidraw assets (dev-docs, examples, firebase, codesandbox) |

Main development: `app/` (including `app/editors/`) + `server/` + `packages/`.
